# PRODIGY_CS_02
Python + Pillow tool that encrypts/decrypts images using pixel XOR with a numeric key. Same key reverses process perfectly. CLI menu lets users encrypt/decrypt images, specify file paths, and save results.
